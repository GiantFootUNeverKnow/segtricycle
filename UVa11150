
#include <iostream>
#include <stdio.h>
#include <stdlib.h>

using namespace std;

int main()
{
	//freopen("Text.txt", "r", stdin);
	int n;
	while (cin >> n) {
		int k = n / 2;
		if (n % 2 == 0) {
			cout << 3 * (k) << endl;
		}
		else {
			cout << 3 * k + 1 << endl;
		}
	}
    return 0;

}

